import React from 'react';

const Input = () => {
    return (
        <div>
            <input type='text' placeholder='Search' className='p-2 rounded-2xl focus:outline-none placeholder:' />
        </div>
    );
};

export default Input;
